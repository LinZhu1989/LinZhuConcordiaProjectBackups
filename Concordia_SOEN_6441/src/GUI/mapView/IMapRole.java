package GUI.mapView;

import utility.IMovable;

/**
 * this interface is used to describe the character inside the map block 
 * @author Jingang.Li
 *
 */
public interface IMapRole extends IMapItem, IMovable {	
}